Pick and load up a stock image
Break it into window sized images
Preprocess them based on edges
Load those edges into the existing program

distribution placement
distribution w/entropy

drawing code

Need:
	Counts array
	tile_hash -> index (for killing duplicates)
	tiles array = //{image, hash, top, bottom, etc}

	we pull the tile from tiles:
		look at 